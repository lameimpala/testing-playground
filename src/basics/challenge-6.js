/*

    Challenge:
        Write a function that returns a given key's value in a given object, and returns null if the value does not exist

    Examples:
        getKeysValueFromObject('myKey', { myKey: 'this is the value of my key' } ) = 'this is the value of my key'
        getKeysValueFromObject('myKeyThatIsNotInTheObject', { myKey: 'this is still the value of my key' } ) = null

*/

export function getKeysValueFromObject(key, object) {
    throw new Error('Not Implemented');
}