/*
    Write a function that adds up the numbers between 0 and the given number, including the given number.

    Examples:
        addUp(2) = 3 because 1 + 2
        addUp(3) = 6 because 1 + 2 + 3
        addUp(-1) = -1 because -1 
        addUp(-4) = -10 because -1 + -2 + -3 + -4

    Notes:
        All inputs are numbers
*/

export function addUp(number) {
    throw new Error('Not Implemented');
}