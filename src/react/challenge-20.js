/*************************
    *** Coding Challenge 20

        Create a component that uses axios to make a GET request to a random endpoint when a "Click Me" button is clicked
        While the request is being made, the loading spinner should be shown instead of the button

        Do not modify the existing loading spinner
*/

export function PostComponent() {
  const loadingSpinner = <div data-testid="loading-spinner" />;

  return null;
}
